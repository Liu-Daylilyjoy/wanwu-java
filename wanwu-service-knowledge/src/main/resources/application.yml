server:
  port: 8083

spring:
  application:
    name: wanwu-service-knowledge

dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  protocol:
    name: dubbo
    port: 20883
  registry:
    address: ${DUBBO_REGISTRY_ADDRESS:N/A}
  consumer:
    check: false
    timeout: 5000
  provider:
    timeout: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info
