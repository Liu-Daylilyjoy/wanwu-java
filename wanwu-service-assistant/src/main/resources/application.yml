server:
  port: 8085

spring:
  application:
    name: wanwu-service-assistant

dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  protocol:
    name: dubbo
    port: 20885
  registry:
    address: ${DUBBO_REGISTRY_ADDRESS:N/A}
  consumer:
    check: false
    timeout: 5000
  provider:
    timeout: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info
