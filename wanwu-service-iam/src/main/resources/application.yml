server:
  port: 8081

spring:
  application:
    name: wanwu-service-iam

dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
    register-mode: ${DUBBO_REGISTER_MODE:instance}
  protocol:
    name: dubbo
    port: 20881
  registry:
    address: ${DUBBO_REGISTRY_ADDRESS:N/A}
  consumer:
    check: false
    timeout: 5000
  provider:
    timeout: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info
