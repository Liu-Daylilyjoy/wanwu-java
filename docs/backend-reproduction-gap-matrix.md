# Wanwu Backend Reproduction Gap Matrix

Date: 2026-07-04

## Scope Baseline

The original Go backend is a multi-service system, not just the Agent loop.

Measured from `D:\work\week3\wanwu`:

- HTTP routes registered by Go BFF: 461
- Proto services: 17
- Proto RPC methods: about 358
- Gorm model structs under service `client/model`: 98
- Runtime services: bff, iam, app, assistant, agent, model, knowledge, rag, mcp, operate, plus callback and sandbox sidecars

Measured from this Java repo before the full reproduction pass:

- `wanwu-service-app`: 25 Java files, 1 test file
- `wanwu-service-bff`: 6 Java files, 2 test files
- Most other service modules: service-info controller + placeholder Dubbo implementation only
- Real business coverage: Agent create/edit/delete/copy/publish/version/conversation/recommend/OpenURL

## Route Groups From Go BFF

| Group | Go routes | Java status |
| --- | ---: | --- |
| `callback/router.go` | 31 | Partially covered; `/callback/v1` file/model/workflow/MCP/chatflow/agent/RAG/WGA/app/skill route shapes now return stable development responses and stream shells through Docker Nginx, model chat can proxy OpenAI-compatible non-stream JSON plus `stream:true` SSE with Go-style active-model and request `model` validation, chat `image_url.url` and multimodal embeddings/rerank file URL conversion, chat stream/non-stream plus embeddings/rerank usage statistics through the callback endpoint, and workflow tool/MCP/Skill callback metadata now reads Java `McpService` with Go-style `skillList` responses; Go callback routes do not show a unified token/signature middleware in the checked source, while individual handlers still forward request auth headers where Go does; provider-specific execution, RAG recall, and sandbox runtime remain missing |
| `openapi/router.go` | 11 | Partially covered; public `/service/api/openapi/v1` agent/conversation/chat/RAG/workflow/chatflow/model/knowledge/MCP/OAuth route shapes now exist with Go-style API-key middleware semantics for missing/disabled/expired keys plus Docker dev-token compatibility, MCP OpenAPI routes validate Go-style `?key=` appKey and `appType=mcpserver`, OpenAPI Agent config/publish now proxy to Java `AppService` with Go-style `AuthModelByUuid` checks, OpenAPI Chatflow conversation/message/SSE routes now persist development conversations through AppService/MySQL with a BFF-local fallback, OpenAPI Knowledge create/select/doc/import/export/hit routes now proxy to Java `KnowledgeService` and enforce the Go router's model UUID plus `AuthKnowledge` permission checks, OpenAPI RAG JSON plus legacy single-frame SSE chat now exposes local `data.searchList` from Java knowledge hits, OpenAPI workflow run returns raw schema-aware output JSON while persisting a workflow run snapshot, and workflow/chatflow upload returns raw URL text like the Go handler, OAuth authorization-code/token/refresh/userinfo runtime is stateful for development apps with HS256 access tokens, RS256 ID tokens, JWKS, and OperateService/MySQL-persisted one-time code plus refresh-token state, OAuth runtime visits feed the Operation client statistic page, and API-key-style OpenAPI calls are persisted through AppService/MySQL with a BFF-local fallback; token streaming, real inference, workflow engine, real MCP runtime, real knowledge indexing, Redis daily statistic sync, OAuth PEM key configuration, and exact runtime cost attribution remain missing |
| `openurl/router.go` | 11 | Mostly covered for Agent OpenURL plus anonymous file upload/merge/clean compatibility; deeper public runtime parity remains partial |
| `v1/api_key.go` | 5 | Covered for create/update/delete/list/status |
| `v1/assistant.go` | 32 | Partially covered; assistant CRUD/config/copy/publish/version/conversation/OpenURL plus local tool/workflow/MCP/skill/multi-agent binding/select compatibility, legacy `/assistant/action` CRUD/enable metadata now persists through Java `AppService`/MySQL in `assistant_actions`, and assistant template list/detail/copy now reads `AppService`/MySQL `app_templates` records with BFF fallback; real assistant-side orchestration still missing |
| `v1/callback.go` | 5 | Partially covered; doc status, deploy info, category info, doc status init, and knowledge status aliases now exist for `/user/api/v1/api/*` and `/api/*`, and doc/report callback status changes now persist into the Java knowledge snapshot; real parser/indexer producers and normalized Go task tables remain missing |
| `v1/chatflow.go` | 10 | Partially covered; Chatflow appspace create/list/copy/import/export/delete with Go-style export attachment headers, `/appspace/workflow/list?appType=chatflow` plus `/appspace/chatflow/list` frontend-list aliases, chatflow-to-workflow conversion through Java AppService/MySQL `apps.app_type`, generic publish/version integration, chatflow application list/detail, conversation-delete compatibility through Java AppService, and OpenAPI conversation/message/SSE chat loop with AppService/MySQL persistence covered; real Coze project conversation runtime and chatflow execution remain missing |
| `v1/common.go` | 48 | Partially covered; app-key, app-list, model selects, IAM-backed user info/language/avatar/password-version compatibility, persisted password hashes after password changes with Go-style strength validation, two-stage email-login development contracts, frontend static docs template downloads, local file upload/check/merge/clean/delete/direct-upload compatibility, classpath-indexed Doc Center tree menu/markdown/search over the Go manual assets, Go-style Markdown image rewriting, and `/user/api/v1/static/manual/**` resource serving covered; several select helpers, real email policy/delivery, avatar object storage, and exact riot search ranking/tokenization still missing |
| `v1/explore.go` | 25 | Partially covered; exploration app list/favorite/history contracts, AppService/MySQL favorite and runtime history records, prompt template, MCP square, and Skill square frontend routes are covered; marketplace ranking and deeper published app runtime remain partial |
| `v1/guest.go` | 13 | Partially covered; workflow template list/detail/recommend/download contracts now read `AppService`/MySQL `app_templates` records with BFF fallback and persist download counts, and email register/reset/login route shapes now return deterministic development responses; real email feature toggles, SMTP/code validation, and IAM email RPC persistence remain missing |
| `v1/knowledge.go` | 79 | Partially covered with Docker MySQL snapshot persistence; knowledge CRUD/list, tags, splitters, local doc import/list/delete/reimport/url-analysis/request-content and BFF-local uploaded UTF-8 file chunking/segment/child-segment CRUD, doc config rebuild, local document hit test, config/import-tip/upload-limit, metadata shell, permissions, QA pair CRUD/list/switch/hit/import-tip/local import/export, persisted export record list/delete/download, community report list/add/update/delete/generate/batch-add compatibility, external API/external knowledge local candidate and mount lifecycle, and local dynamic graph projection for frontend navigation; normalized Go tables, parser-backed file conversion, vector indexing/report generation/RAG integration/Dify external knowledge runtime/MinIO async export missing |
| `v1/mcp_square.go` | 3 | Partially covered; local MCP square list/detail/recommend contracts return deterministic seed data |
| `v1/model.go` | 15 | Partially covered; model list/detail/import/update/delete/status, recommend including type-correct recommendation tags and multimodal embedding/rerank plus ASR/OCR/PDF Parser/GUI seed recommendations, callback model-info redaction plus OpenAI-compatible callback chat stream/non-stream proxying with active/model-name validation, chat image URL and multimodal embeddings/rerank file URL conversion, embeddings/rerank JSON proxying, and local OCR/PDF Parser/GUI/sync-ASR callback response bodies, validate-thinking stub, provider list, select endpoints including Go path aliases for `multi-embedding`, `multi-rerank`, `asr`, sync-ASR/workflow selectors, Docker development ASR stream event route, model experience dialog/list/records/delete plus history-aware, parameter-aware, and usage-aware non-stream OpenAI-compatible upstream answer wrapping with local echo fallback, and global Safety Guard input/output replacement covered for model experience; real XunFei ASR WebSocket proxying, provider streaming inference for model experience, provider-specific callback execution, and provider token-stream safety filtering missing |
| `v1/oauth.go` | 5 | Partially covered; operation OAuth app create/list/update/delete/status covered with a Docker development IAM repository, and OpenAPI OAuth login/authorize/token/refresh/userinfo now runs as a stateful development authorization-code flow with JWT access/id tokens, JWKS, and OperateService/MySQL-persisted one-time code plus refresh-token state; PEM key configuration and normalized IAM OAuth tables remain missing |
| `v1/permission.go` | 23 | Mostly covered for Docker development; user/role/org create/update/delete/list/info/status, role select/template, org user add, users-outside-org, and CSV/TSV/XLSX multipart batch user import are covered with a MySQL-backed JSON compatibility repository. Batch import now accepts Go localized headers, enforces the 500-row cap, validates username/password/phone/company, and stores initial password hashes; admin password reset is covered by the common user controller; normalized Go-equivalent IAM tables, invitations, audit logs, exact localized import error text, and real permission enforcement remain missing |
| `v1/rag.go` | 10 | Partially covered; RAG app draft/create/update/config/copy/delete/list/publish/version, AG-UI draft/published chat, local Java knowledge/QA hit integration, MySQL-persisted chat snapshots, and multipart upload compatibility covered for both user-domain and service-domain frontend access; real vector/rerank retrieval and provider generation still missing |
| `v1/safety.go` | 9 | Partially covered with Docker MySQL snapshot persistence; sensitive word table create/list/detail/update/reply/delete/select plus single and BFF-local XLSX/CSV word import/list/delete covered for frontend safety guard management; Agent/RAG local chat input/output replacement honors global and configured tables, and Model Experience input/output replacement uses global tables; normalized tables, MinIO object lifecycle, rich Excel parity, and real provider token-stream sliding-window filtering missing |
| `v1/setting.go` | 3 | Partially covered; custom tab/login/home writes and `/base/custom` readback now proxy to Java `OperateService` with Docker MySQL snapshot persistence and Go-style non-empty field merge semantics; uploaded asset storage and full light/dark seed parity missing |
| `v1/skill.go` | 28 | Partially covered; custom/built-in/acquired Skill resource CRUD/config, Skill square list/detail/share/download, Skill select, and local Skill conversation SSE/save shell covered; real skill package parsing, LLM generation, and persistence missing |
| `v1/statistic.go` | 14 | Partially covered; app/model/API Key dashboard select, overview, trend, list, record, and xlsx export compatibility routes are covered; API Key statistics and app/model statistics now reflect AppService/MySQL-persisted runtime calls, including OpenURL `webURL` source counts, with BFF-local fallbacks where applicable; Go-style Redis cron sync and exact provider-reported runtime costs/tokens remain missing |
| `v1/statistic_client.go` | 1 | Partially covered; `/statistic/client` returns the frontend-compatible overview/trend contract backed by Java `OperateService` OAuth client records with BFF-local browse compatibility; normalized Go client/daily tables and Redis global browse aggregation remain missing |
| `v1/tool.go` | 39 | Partially covered with Docker MySQL snapshot persistence; custom tool CRUD/list/detail/schema parsing, built-in tool square/detail/API-key shell, MCP import/server/tool binding/OpenAPI-tool shell, assistant and workflow tool/MCP select/action compatibility, prompt custom/template CRUD/copy and local SSE optimize/reason/evaluate covered; real remote MCP/tool execution and normalized resource tables still missing |
| `v1/wga.go` | 31 | Partially covered; `/service/api/v1/general/agent` WanwuBot/general-agent config now persists through Java `AppService`/MySQL in `general_agent_configs`, conversation/run/Skill-preview generated-workspace snapshots persist through `general_agent_conversations`, and resource select, conversation list/detail/config, AG-UI SSE chat, workspace preview/download, Skill conversation import/convert/refresh/preview, and HITL reply/reject contracts are covered for frontend access, with ontology-agent config/resource exposure intentionally omitted; real WGA sandbox execution, persistent workspace artifact files, OpenClaw, normalized Go-equivalent runtime tables, and sub-agent orchestration remain missing |
| `v1/workflow.go` | 20 | Partially covered; Workflow appspace create/list/copy/import/export/delete with Go-style export attachment headers, workflow-to-chatflow conversion through Java AppService/MySQL `apps.app_type`, template copy, generic publish/version integration, MySQL-persisted local run snapshots with schema-aware output, assistant workflow select, `/appspace/workflow/model/select/{modelType}` user/org scoped model selection with Go-style ASR aliasing, `/workflow/tool/select`, `/workflow/tool/action`, `/workflow/tool/box` MCP-backed compatibility, `/workflow/api` use/openapi-schema plus schema-driven parameter/input/output compatibility, and avatar upload route covered; full visual workflow editor engine, node execution, and Coze execution still missing |

## Additional Frontend Proxy Families

| Proxy family | Java status |
| --- | --- |
| `nginx /use/model/api/v1` | Partially covered; legacy frontend `MODEL_API` assistant create/update/delete/publish/list/info, auto-create to Java `AppService` drafts, assistant conversation create/list/detail/delete, assistant knowledge-file upload/list/delete metadata now persists through Java `AppService`/MySQL in `assistant_knowledge_files`, ChatLLM/CUBM conversation create/list/detail/delete now persist through Java `AppService`/MySQL using the shared conversation repository, assistant action create/update/info/delete metadata now persists through Java `AppService`/MySQL in `assistant_actions`, model experience dialog aliases, file extract, file confirm, and model expansion batch upload now return frontend-compatible responses through `WanwuModelUseApiController`; real model-use inference service, CUBM runtime, action execution, model-generated auto design content, and legacy assistant knowledge-file content/object-storage lifecycle remain missing |
| `nginx /service/api/v1` | Partially covered; service-domain RAG upload alias now supports the frontend `${SERVICE_API}/rag/upload` caller without frontend changes, while real object storage, parser/indexer callbacks, and RAG pipeline integration remain missing |

## Proto Service Groups

| Proto service | RPC count | Java status |
| --- | ---: | --- |
| `AppService` | 41 | Partially covered for assistant CRUD/config, publish/version/OpenURL, OpenAPI Agent config/publish, legacy `/use/model/api/v1` assistant aliases plus persisted ChatLLM/CUBM conversation state, assistant knowledge-file metadata, assistant action metadata, WGA global config metadata, and WGA conversation/run/generated-workspace snapshot metadata, app keys/API keys including appKey lookup by key, persisted local assistant resource bindings, AppService/MySQL-backed assistant and Workflow template rows, RAG app lifecycle plus local knowledge-hit-backed chat events and chat snapshot persistence, Agent/RAG local safety input/output replacement, Workflow app lifecycle/import/export/schema-aware run snapshot persistence, Chatflow lifecycle/application shell plus OpenAPI conversation/message persistence, Safety guard management shell, frontend statistics dashboard compatibility, OpenAPI API Key statistic record/query backed by MySQL, and app/model statistic record/query/list backed by MySQL; real RAG vector/rerank/model orchestration, real Workflow/Chatflow engine execution, legacy model-use/CUBM runtime, real WGA sandbox/workspace artifact persistence, Redis statistic sync, exact provider metrics, and real token-stream safety filtering missing |
| `AssistantService` | 67 | Partially covered through Java `AppService`; local workflow/MCP/tool/skill/multi-agent binding state, legacy assistant action alias CRUD/enable metadata persisted through `assistant_actions`, assistant template copy, and WGA/WanwuBot frontend conversation shell covered, while real assistant-side orchestration, prompt runtime, WGA sandbox execution, and real skill conversations remain missing |
| `IAMService` | 49 | Development login, password-change hash persistence and Go-style password strength validation, frontend permission split, IAM user/role/org read-write compatibility, Go-style batch import validation with initial password hash persistence, legacy platform custom setting fallback, and OAuth app management covered with a Docker MySQL JSON compatibility repository; normalized IAM/OAuth schema parity, email verification policy, richer Excel parser parity, invitations, and audit logs still missing |
| `KnowledgeBaseDocService` | 26 | Partially covered with Docker MySQL snapshot-backed doc import/list/delete/reimport/url-analysis, callback-driven doc/report status persistence, request-content and BFF-local uploaded UTF-8 file chunking including custom splitter and parent-child settings, doc config rebuild, segment create/update/delete/status/labels, child segment create/list/update/delete, and local ZIP export records; parser-backed file conversion, vector indexing, MinIO async exports, asynchronous reimport workers, and real async parser/indexer workers missing |
| `KnowledgeBaseKeywordsService` | 5 | Partially covered with Docker MySQL snapshot-backed create/list/detail/update/delete, knowledge association, and document-page keyword echo; normalized `knowledge_keywords` table and RAG keyword sync remain missing |
| `KnowledgeBasePermissionService` | 6 | Partially covered with owner/admin/user/org frontend compatibility |
| `KnowledgeBaseQAService` | 10 | Partially covered with Docker MySQL snapshot-backed QA pair create/update/delete/list/switch/import-tip, request-content and BFF-local uploaded UTF-8 CSV/TSV import parsing, local CSV export record, and local text hit; asynchronous import tasks, MinIO async exports, and real vector/keyword search missing |
| `KnowledgeBaseReportService` | 6 | Partially covered with Docker MySQL snapshot-backed report list/add/update/delete plus deterministic local generate, request-content and BFF-local uploaded UTF-8 CSV/TSV batch-add parsing, and frontend upload placeholder fallback; real graph-derived report generation, RAG report storage, and async import task execution missing |
| `KnowledgeBaseService` | 28 | Partially covered with Docker MySQL snapshot-backed CRUD/list, local document-segment hit test, local graph projection, external API, external knowledge mount lifecycle, OpenAPI Knowledge proxy coverage, and Go-style explicit knowledge permission checks for OpenAPI routes; real RAG/vector/rerank retrieval, LLM/RAG graph extraction, and normalized tables missing |
| `KnowledgeBaseSplitterService` | 4 | Partially covered with preset and custom splitter CRUD |
| `KnowledgeBaseTagService` | 6 | Partially covered with tag CRUD/bind/count |
| `MCPService` | 65 | Partially covered with Java RPC contract and Docker MySQL snapshot resource repository for custom tools, built-in tools, custom MCP, MCP Server tool binding, MCP square seeds, workflow tool select/action/box adapters, callback metadata lookup for workflow tools/MCP/Skill, custom prompts/templates, local prompt SSE, custom/built-in/acquired skills, Skill square, and Skill conversation shell; real MCP protocol runtime, external API invocation, real skill package execution, and normalized tables missing |
| `ModelService` | 16 | Partially covered with Java RPC contract and Docker MySQL JSON compatibility repository for model management/select/recommend/provider flows, including Qwen multimodal embedding/rerank plus YuanJing OCR/PDF Parser/GUI development seeds for knowledge upload selectors, plus `/user/api/v1`, legacy `/use/model/api/v1` model experience dialog/record persistence aliases, `/model/experience/llm` history-aware, parameter-aware, and usage-aware non-stream OpenAI-compatible upstream answer wrapping with local echo fallback, `/callback/v1/model/{modelId}` metadata redaction, OpenAI-compatible callback chat stream/non-stream proxying with active/model-name validation, chat image URL plus multimodal embeddings/rerank file URL data conversion, and usage statistics, embeddings/rerank JSON proxying with usage statistics, and local OCR/PDF Parser/GUI/sync-ASR callback response bodies; normalized Go-equivalent model tables, streaming/provider-specific inference, encrypted credentials, provider-specific callback execution, and provider metrics still missing |
| `OperateService` | 6 | Partially covered; custom system tab/login/home config, get-system-custom, add-client-record, get-client-statistic, and OpenAPI OAuth code/refresh-token runtime records are implemented with Docker MySQL snapshot persistence, mode isolation, OAuth runtime recording, and non-empty field merge semantics; normalized Go client/daily tables, uploaded asset lifecycle, and Redis daily aggregation missing |
| `PermService` | 2 | Missing as independent service |
| `RagService` | 15 | Partially covered through Java `AppService` and BFF for appspace RAG lifecycle/config/publish/version/copy, frontend AG-UI chat shell with MySQL-persisted snapshots, OpenAPI JSON and legacy single-frame SSE search-list output, upload response shape, and local knowledge/QA hit search-list generation; standalone Go-equivalent retrieval/rerank/model token-stream behavior still missing |
| `SafetyService` | 12 | Partially covered with Java RPC contract and Docker MySQL snapshot repository for table CRUD/reply/select plus single and BFF-local XLSX/CSV word import/list/delete; Agent/RAG local chat input/output checks consume global tables and enabled personal tables, and Model Experience input/output checks consume global tables; normalized Go tables, MinIO object lifecycle, richer Excel parity, and real provider token-stream interception missing |

## Reproduction Order

1. Platform and compatibility shell: common response, auth context, file upload, appspace app list, app keys, API keys.
2. IAM: login, password-change hash persistence, user/org/role/permission, batch user import validation, OAuth app management. User/org/role/OAuth state now survives Docker restarts through MySQL; normalize IAM tables, email verification policy, richer Excel parser parity, invitations, and audit logs next.
3. Model: model import/list/select/status/delete and model experience dialog.
4. Knowledge: knowledge base CRUD, docs, QA, tags, splitters, permissions, reports, local graph projection, callback status updates.
5. MCP/tool/prompt/skill: custom tools, MCP servers/tools, prompt templates, built-in/custom/acquired skills. Tool/MCP/Prompt/Skill management now has a Docker MySQL snapshot-backed frontend-compatible loop, including workflow tool select/action/tool-box adapters; real MCP execution, skill package execution, and normalized tables remain next.
6. Safety: sensitive word table/word management, BFF-local uploaded XLSX/CSV import, Agent/RAG local chat input/output replacement, and Model Experience global input/output replacement now have a Docker MySQL snapshot-backed loop; normalized tables, MinIO object lifecycle, richer Excel parity, and real provider token-stream interception remain next.
7. Setting: platform tab/login/home custom config now has an OperateService-backed Docker MySQL write/read loop; uploaded asset lifecycle and full theme seed parity remain next.
8. Operation: OAuth app management, development OAuth authorization-code/JWT/JWKS runtime with OperateService/MySQL-persisted code and refresh-token records, and OperateService-backed OAuth client statistics are covered; PEM key configuration, normalized client/daily tables, and Redis global browse statistics remain next.
9. Exploration square: app list/favorite/history, AppService/MySQL favorite and runtime history records, and MCP/Prompt/Skill square frontend contracts are covered; real marketplace ranking remains next.
10. Statistics dashboard: app/model/API Key select, overview, trend, list, record, and xlsx export compatibility are covered; API Key statistics and app/model statistics now consume AppService/MySQL-persisted runtime calls, including OpenURL webURL source counts, with BFF-local fallbacks where applicable, while Go-style Redis statistic sync and exact provider metrics remain next.
11. Templates: assistant template and workflow template list/detail/recommend/download/copy contracts are covered with AppService/MySQL seed persistence, persisted workflow-template download counts, and BFF fallback; real marketplace ranking, publication flow, and template governance remain next.
12. File upload: chunk upload/check/list/merge/clean/delete, direct upload, proxy upload, inferpub upload, and OpenURL anonymous upload contracts are covered with local BFF storage; MinIO/object lifecycle remains next.
13. Common user/doc center: user info plus password/avatar/language updates now proxy to Java IAM and survive Docker restarts through the IAM JSON compatibility repository; password changes persist a hash and enforce the Go-style length/letter/number/special-character rule; email register/reset/two-stage-login route contracts, frontend static docs templates, classpath-indexed doc-center tree menu/markdown/search/entry over 725 Go manual files, Go-style Markdown image rewriting, and static manual resource serving are covered with development data; email delivery/code validation, avatar object storage, and exact riot search ranking/tokenization remain next.
14. Legacy model-use API: frontend `/use/model/api/v1` assistant, auto-create-to-draft, assistant knowledge-file upload/list/delete metadata persistence, assistant action create/update/info/delete metadata persistence, ChatLLM/CUBM AppService/MySQL conversation persistence, model-experience, and legacy upload/confirm route contracts are covered; real model-use inference, CUBM runtime, action execution, model-generated auto design content, and legacy assistant knowledge-file content/object-storage lifecycle remain next.
15. RAG: draft/publish/chat/copy/version and knowledge integration. Management lifecycle, frontend chat shell, MySQL-persisted chat snapshots, user/service-domain upload response shapes, and local knowledge/QA search-list generation are covered; real vector/rerank retrieval and provider generation remain next.
16. Assistant full surface: Workflow app lifecycle and schema-aware run snapshot persistence are now covered as a local shell; continue with deeper runtime orchestration, real node execution, and select endpoints.
17. Guest/callback/openapi: public API compatibility shells are covered for the main Go OpenAPI and callback route families, with Agent config/publish proxying into Java AppService, Chatflow OpenAPI conversation/message state persisting through Java AppService/MySQL with a BFF-local fallback, Knowledge OpenAPI proxying into the Java KnowledgeService local loop, workflow run/upload raw response parity, callback workflow-tool/MCP/Skill metadata lookup through Java McpService, callback doc/report status persisting into the Java knowledge snapshot, API Key statistics recording AppService/MySQL-persisted OpenAPI calls, OpenAPI API-key middleware semantics for missing/disabled/expired keys, fine-grained AuthKnowledge/AuthModel route checks, and MCP OpenAPI appKey query validation; real public execution, callback auth/signature checks, provider execution, Redis statistic sync, and real parser/indexer callback producers remain next.
18. WGA/general agent: WanwuBot frontend routes are now exposed through a Java compatibility shell, and global config plus conversation/run/generated-workspace snapshots survive Docker restarts through AppService/MySQL; continue with real WGA sandbox execution, durable artifact files, normalized runtime tables, OpenClaw, and sub-agent orchestration.

## Development IAM Accounts

The Java IAM service currently exposes two Docker development accounts:

- `admin` / token `dev-token`: implemented stable permissions only: `permission`, `permission.user`, `permission.org`, `permission.role`, `setting`, `model`, `model.model_management`, `app`, `app.rag`, `app.workflow`, `app.agent`, `api_key`, `api_key.api_key_management`, `resource`, `resource.knowledge`, `resource.tool`, `resource.mcp`, `resource.prompt`, `resource.skill`, `resource.safety`, `operation`, `operation.oauth`, `operation.statistic_client`, `exploration`, `exploration.app`, `exploration.mcp`, `exploration.template`, `exploration.skill`, `app_observability`, `app_observability.statistic`, `wga`, `wga.wanwu_bot`. Email auth compatibility routes also map back to this token for the `admin` development account.
- `app` / token `dev-token-app`: `app`, `app.rag`, `app.workflow`, and `app.agent` only.

Unreproduced frontend modules are intentionally not exposed to avoid `Not Found` toasts from pages whose Java backend routes are not implemented yet. Permission management now has a Docker development user/org/role write loop, including create/update/delete/status and CSV/TSV/XLSX multipart batch user import with Go localized headers, row cap, username/password/phone/company validation, and imported password hash persistence; those mutable IAM/OAuth records survive restarts through `iam_service.iam_records`, while normalized Go-equivalent IAM tables, exact localized import error text, invitations, and audit logs remain later. Common user/profile routes currently cover IAM-backed development user info, password-version/avatar/language update persistence, password-change hash persistence with Go-style strength validation, admin password reset compatibility, email register/reset/two-stage-login development route contracts, frontend static template downloads, Go manual backed Doc Center tree menu/content/search with Markdown image rewriting, and static manual resource serving; real email delivery/code validation, avatar object storage, and exact riot search ranking/tokenization remain later. The `setting` permission is exposed because the platform setting page can now write custom tab/login/home config to `OperateService` and read it back through `/base/custom` from `operate_service.operate_records`. Operation is exposed because OAuth app management, development OAuth authorization-code/refresh-token runtime persisted through OperateService/MySQL, and OperateService-backed OAuth client statistics are now available for the operation management page. Exploration is exposed because app square list/favorite/history, AppService/MySQL favorite records, published Agent/RAG/Workflow/Chatflow runtime history readback, MCP square, prompt template square, Skill square, and AppService/MySQL-backed assistant and workflow template contracts are available. App Observation is exposed because the statistics dashboard can now load app/model/API Key selects, overview cards, trend charts, tables, details, and xlsx export buttons without 404s, and API Key plus app/model statistics now reflect AppService/MySQL-persisted runtime calls including OpenURL webURL source counts; Go-style Redis statistic sync and exact provider metrics remain later. Model Management is exposed because model CRUD/status/select/provider/recommend, Go selector aliases for `multi-embedding`, `multi-rerank`, and `asr`, Qwen multimodal embedding/rerank plus YuanJing OCR/PDF Parser/GUI development seeds for knowledge-upload selectors, model-experience history, global Safety Guard input/output replacement for model-experience LLM, and legacy `/use/model/api/v1` model-experience aliases now use a Docker MySQL compatibility repository; normalized model tables and real provider inference remain later. Legacy `MODEL_API` assistant/ChatLLM/CUBM route contracts are exposed through a development BFF compatibility shell, with ChatLLM/CUBM conversations and assistant knowledge-file metadata persisted through AppService/MySQL; real model-use inference, CUBM runtime, and legacy file content/object-storage lifecycle remain later. Knowledge is exposed because the frontend can now create/list/update/delete internal and external knowledge bases and use knowledge tags, splitters, docs, request-content and BFF-local uploaded UTF-8 chunks, segments, local document hit tests, local graph projection, permissions, QA pairs, community reports, static import templates, and external API/dataset selection through a Docker MySQL snapshot repository; normalized Go knowledge tables, parser-backed file conversion/indexing, LLM/RAG graph extraction, graph-derived report generation, real Dify integration, and real vector/rerank retrieval remain later slices. Tool, MCP, Prompt, and Skill are exposed because the resource center now has Docker MySQL snapshot-backed Java management loops and frontend-compatible select/action/config/conversation contracts, including Workflow editor tool select/action/tool-box compatibility. Safety is exposed because the safety center now has a Docker MySQL snapshot-backed Java management loop, single and uploaded XLSX/CSV sensitive word import/list/delete, Agent/RAG local chat input/output replacement, Model Experience global input/output replacement, and a downloadable static word-import template. File upload is exposed through `/service/api/v1`, legacy model-use aliases, and OpenURL aliases with BFF-local development storage; Knowledge Doc/QA/Report imports consume UTF-8 uploaded files from this store, and Safety Guard imports consume uploaded XLSX/text files from the same store, while MinIO parity remains later. Public OpenAPI is exposed through `/service/api/openapi/v1` with Go-style API-key middleware semantics for required/disabled/expired keys plus Docker development-token compatibility, fine-grained AuthModel/AuthKnowledge route checks for model and knowledge fields, workflow run raw JSON plus workflow/chatflow upload raw URL response parity, MCP appKey query validation for `mcpserver` app keys, RAG/workflow/model compatibility shells, and a stateful OAuth authorization-code flow whose code/refresh-token state persists through OperateService/MySQL and whose visits record OperateService client-statistic data, while Agent config/publish now proxies into the Java AppService draft/version loop, Chatflow OpenAPI now has an AppService/MySQL conversation/message/SSE chat loop with a BFF-local fallback, Knowledge OpenAPI now proxies create/select/doc/import/export/hit calls into the Java KnowledgeService local loop, and API-key-style OpenAPI calls are visible in the statistics dashboard through AppService/MySQL persistence; true public runtime parity remains later. RAG is exposed because management plus draft/published chat, MySQL-persisted chat snapshots, local knowledge/QA search-list events, safety input blocking, and upload response shape are now available. Workflow and Chatflow are exposed because appspace lifecycle, template copy, import/export, publish/version, local run shell, workflow tool select/action/tool-box compatibility, `/workflow/api` compatibility, chatflow application compatibility, OpenAPI Chatflow local conversation compatibility, and avatar upload compatibility are now available. WanwuBot/WGA is exposed because `/service/api/v1/general/agent` now covers the frontend-visible config/resource/conversation/SSE/workspace/Skill/HITL contracts. The ontology agent menu remains out of this Java reproduction scope. Details are tracked in `docs/development-login-accounts.md`. This is a development compatibility slice, not the final reproduced Go IAM/model/knowledge/MCP/Safety/WGA persistence and execution model.

WGA global config now survives Docker restarts through AppService/MySQL `general_agent_configs`; WGA conversation, run, Skill preview, and generated workspace snapshot state survives Docker restarts through AppService/MySQL `general_agent_conversations`. Real sandbox artifact files and normalized Go-equivalent WGA runtime tables remain later slices.

## Operating Rule

Each slice must:

- Start from Go source behavior and frontend API callers.
- Add failing tests first.
- Implement Java service/BFF/persistence as needed.
- Add or update technical notes.
- Run Docker Maven tests and Docker Compose smoke tests when the slice touches runtime routes.
- Commit and push independently.
